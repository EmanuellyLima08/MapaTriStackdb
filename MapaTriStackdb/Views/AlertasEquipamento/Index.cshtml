@model IEnumerable<MapaTriStackdb.Models.AlertaEquipamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Equipamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoAlerta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mensagem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataAlerta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Equipamento.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoAlerta.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mensagem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataAlerta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AlertaEquipamentoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AlertaEquipamentoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AlertaEquipamentoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
